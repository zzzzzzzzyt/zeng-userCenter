spring:
  application:
    name: user-center
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zeng?serverTimezone=UTC
    username: root
    password: root
  session:
    timeout: 86400

#线上的环境  只用改掉和公共环境不同的配置 即可